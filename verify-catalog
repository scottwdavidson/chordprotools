#!/bin/zsh
# Verify that every song-catalog.csv entry matches its .cho file.
# Reports MISSING FILE and DRIFT. Exits 1 if any issues are found.
#
# Usage:
#   ./verify-catalog

"${0:a:h}/cpt" verify-catalog "$@"
